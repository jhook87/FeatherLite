# ───────────────────────────────
# Core Environment
# ───────────────────────────────
NODE_ENV="development"
SKIP_ENV_VALIDATION="false"
NEXT_TELEMETRY_DISABLED="1"

# ───────────────────────────────
# Database (PostgreSQL via Prisma)
# ───────────────────────────────
# Replace USER, PASSWORD, HOST, PORT, and DATABASE with your actual connection details
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# ───────────────────────────────
# Shopify Storefront Configuration
# ───────────────────────────────
SHOPIFY_STORE_DOMAIN="featherlitemakeup.myshopify.com"
SHOPIFY_STOREFRONT_ACCESS_TOKEN="storefront-access-token"
SHOPIFY_STOREFRONT_API_VERSION="2024-04"

# ───────────────────────────────
# Shopify Admin Configuration
# ───────────────────────────────
SHOPIFY_ADMIN_ACCESS_TOKEN="admin-access-token"
SHOPIFY_ADMIN_API_VERSION="2024-07"
SHOPIFY_WEBHOOK_SECRET="webhook-secret"

# ───────────────────────────────
# Admin Review & Moderation Login
# ───────────────────────────────
REVIEW_ADMIN_EMAIL="admin@featherlite.test"
# For production, hash your password:
# echo -n "your-password" | openssl dgst -sha256 -hex | awk '{print $2}' | xargs -I{} printf "sha256:%s\n" {}
REVIEW_ADMIN_PASSWORD_HASH="sha256:replace-with-64-character-hex-digest"
# 32+ character secret for signing admin sessions
REVIEW_ADMIN_SECRET="replace-with-random-secret"

# ───────────────────────────────
# Optional Legacy Variables (fallbacks)
# ───────────────────────────────
# ADMIN_EMAIL="admin@featherlite.test"
# ADMIN_PASSWORD_HASH="plain:local-dev-only"
# NEXTAUTH_SECRET="replace-with-random-secret"

# ───────────────────────────────
# Deployment / Safety Toggles
# ───────────────────────────────
# Allow seeding only when intentionally set to true
ALLOW_PRISMA_SEED="false"
